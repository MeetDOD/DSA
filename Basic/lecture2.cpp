#include <iostream> //lib
using namespace std;
// You are using it, namespace is namespace, std is the standard library where instead of std::cout we only write cout.
int main()
{
    cout << "GOD IS GOOD" << endl; //; => ending line like '.' in english sentence
    cout << "\n";                  // for new line
    cout << "GOD IS GREAT";
}

// max = 2^32-1
// min = 0